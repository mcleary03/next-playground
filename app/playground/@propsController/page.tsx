// This is the default slot for the props controller component and only seen on first load before user selects a component to view
const PropsControllerSlot = () => <div className="mx-10">
  <p>Props can be controlled in this section.</p>
</div>

export default PropsControllerSlot
