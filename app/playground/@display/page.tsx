import Heading from "@/components/Heading"

// This is the default slot for the display component and only seen on first load before user selects a component to view
const DisplaySlot = () => <>
  <Heading heading="Display slot" />
</>

export default DisplaySlot
